// Optional: Node.js routes (if you have multiple endpoints)

const express = require('express');
const router = express.Router();

// Example route
// router.get('/example', (req, res) => {
//   res.json({ message: 'Example route' });
// });

module.exports = router;

